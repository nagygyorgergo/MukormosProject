import { ExpiredDirective } from './expired.directive';

describe('ExpiredDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpiredDirective();
    expect(directive).toBeTruthy();
  });
});
